
export const checkers = [];

/*
export function canMoveChecker(id, checkerPos, toX, toY) {
  const [x, y] = checkerPos
  const dx = toX - x
  const dy = toY - y
  return (
    (Math.abs(dx) === 1 && Math.abs(dy) === 1)
  )
}

export function moveChecker(id, toX, toY) {
  checkerPos = [toX, toY]
}*/

/*

?How to move checkers?

*/